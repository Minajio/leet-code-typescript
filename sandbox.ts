/**
 * Simple playground for quick experiments. Replace the example below with any code you want to try.
 */
function runSandbox(): void {
  const nums = [1, 2, 3, 4];
  const doubled = nums.map((n) => n * 2);
  console.log('Sandbox ready. Example output:', doubled);
}

runSandbox();
